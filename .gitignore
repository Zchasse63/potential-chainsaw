# secrets — never commit
.env
.env.*
# ...but the committed template (names only, no values) is tracked on purpose.
!.env.example

# dependencies & build output
node_modules/
dist/
dist-test/
.turbo/
*.tsbuildinfo

# supabase local
supabase/.temp/
supabase/.branches/

# glofox scrape intermediates (regenerable source material)
docs/glofox/_*

# os
.DS_Store
