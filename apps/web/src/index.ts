/**
 * @kelo/web — placeholder so the workspace resolves and the typecheck graph is
 * complete; src/ is intentionally empty of implementation. The real operator app
 * (Vite + React SPA, TanStack Router + Query — plan-final §1) is a later unit.
 */
export {};
