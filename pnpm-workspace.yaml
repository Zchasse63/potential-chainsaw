packages:
  - "packages/*"
  - "apps/*"
  # @kelo/workers is a single package at workers/ (plan-final §1); the workers/*
  # glob stays so future split-out packages are picked up without editing this file.
  - "workers"
  - "workers/*"
