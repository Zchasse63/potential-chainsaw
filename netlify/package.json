{
  "name": "@kelo/netlify-functions",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Netlify Function entrypoints (plan-final §1): the /api/* Hono adapter, the SINGLE scheduler tick, and the secret-gated worker background function.",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@kelo/api": "workspace:*",
    "@kelo/db": "workspace:*",
    "@kelo/workers": "workspace:*",
    "hono": "4.12.30"
  },
  "devDependencies": {
    "@netlify/functions": "5.3.0"
  }
}
