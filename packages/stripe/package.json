{
  "name": "@kelo/stripe",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Injectable, dry-run-capable Stripe Connect adapter, webhook signature verifier, and typed event mapper. No live calls until an account exists. DB-free.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "vitest": "4.1.10"
  }
}
